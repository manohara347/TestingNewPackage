import XCTest
@testable import TestingNewPackage

final class TestingNewPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TestingNewPackage().text, "Hello, World!")
    }
}
